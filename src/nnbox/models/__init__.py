"""Full models. Imported explicitly, e.g. ``from nnbox.models.vit import ViT``.

Deliberately not re-exported from the top-level package: models are heavier and
more opinionated than the layers, so they stay behind an explicit import.
"""
